# don't publish the private key, for obvious reasons
/id_rsa
# leaking hostnames from known_hosts is also not a great idea
/known_hosts
# ssh config also has hostnames (for shorter aliases) and usernames
/config
